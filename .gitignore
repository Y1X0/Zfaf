node_modules/
.pnpm-store/
dist/
build/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

# Playwright writes these under the app that owns the tests, not the repo root.
playwright-report/
test-results/
blob-report/

.vercel
.idea/
.vscode/*
!.vscode/extensions.json

# Fetched on demand by `pnpm --filter @zfaf/media-processing fixtures:heic`; not vendored
# because we will not commit a third-party photograph of unclear licence.
packages/media-processing/src/__fixtures__/

# Emitted by `apps/web/scripts/build-public-script.mjs` from the TypeScript in
# `apps/web/src/public-page/`, which is the source of record.
apps/web/public/invitation.js
